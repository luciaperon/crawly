import { Crawler } from "../../src/Services/Crawler/Crawler";
import { describe, it } from "vitest";

describe("Test basic crawl", () => {
  let crawler = new Crawler();

  it("Should not fail on valid job body initialization", () => {});
});
